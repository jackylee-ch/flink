<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.apache.flink</groupId>
		<artifactId>flink-state-backends</artifactId>
		<version>2.2.0</version>
	</parent>

	<artifactId>flink-statebackend-forst-rs</artifactId>
	<name>Flink : State backends : ForSt-RS (Rust + JDK 25 FFM)</name>

	<packaging>jar</packaging>

	<properties>
		<!--
			Pin source/target=25 unconditionally for this module. The parent
			Flink build defaults to Java 17 (parent pom.xml line 134-135);
			we override here because FFM (Linker.nativeLinker(),
			Linker.Option.critical(true)) is JDK 25+ only. The auto-activated
			forst-rs-jdk25 profile in flink-state-backends/pom.xml ensures
			this module is only included in the reactor when JDK >= 25, so
			these settings never collide with a JDK 17 build.
		-->
		<source.java.version>25</source.java.version>
		<target.java.version>25</target.java.version>
		<maven.compiler.source>25</maven.compiler.source>
		<maven.compiler.target>25</maven.compiler.target>
		<forstrs.native.libpath>${user.home}/Code/stczwd/ForSt/target/release/libforst_rs_ffi.dylib</forstrs.native.libpath>
		<surefire.module.config><!--
			FFM API requires opt-in for native access in JDK 22+.
			Vector API (jdk.incubator.vector) requires add-modules.
			-->--enable-native-access=ALL-UNNAMED
			--add-modules jdk.incubator.vector
			-Dforstrs.native.libpath=${forstrs.native.libpath}
		</surefire.module.config>
	</properties>

	<dependencies>

		<!-- core dependencies -->

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-streaming-java</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-runtime</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- table-common provides RowData / AsyncTableFunction for the local-lookup function -->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-common</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- test dependencies -->

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
			B-Prod-followup-4: flink-test-utils brings in MiniClusterResource,
			MiniClusterWithClientResource, MiniClusterExtension + the streaming
			and clients test-jar classes needed to drive a job through a
			MiniCluster. Wires the canonical RocksDB-style test scaffolding
			(see flink-statebackend-rocksdb/pom.xml for the upstream pattern).
		-->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-test-utils</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-test-utils-junit</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
			B-Prod-followup-LittleE2E: sibling state-backend modules whose SPI
			factories the perf bench runs against. Pulled in at test scope so
			LittleE2EPerfBench can drive `org.apache.flink.contrib.streaming.state.
			EmbeddedRocksDBStateBackendFactory` and
			`org.apache.flink.state.forst.ForStStateBackendFactory` (which itself
			transitively pulls in org.forst:forstjni containing org.forstdb.*).
			Without these, the bench's `forst` and `forst-via-libforst_rs_ffi`
			variants fail with NoClassDefFoundError: org/forstdb/TickerType (run
			25689814526 on 2026-05-11).
		-->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-statebackend-rocksdb</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-statebackend-forst</artifactId>
			<version>${project.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-streaming-java</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-runtime</artifactId>
			<version>${project.version}</version>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>

		<!--
			JMH is declared in test scope so that the bench harness under
			src/test/java/org/apache/flink/state/forstrs/jmh/ can pick up
			@Benchmark annotations if we ever wire the JMH maven plugin in.
			The ForStCompareBenchmark.java currently runs as a plain main()
			driven by run-jmh-3way.sh — JMH itself is not strictly required
			to execute the bench, but having the deps available makes the
			JMH-plugin path one config block away.
		-->
		<dependency>
			<groupId>org.openjdk.jmh</groupId>
			<artifactId>jmh-core</artifactId>
			<version>1.37</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.openjdk.jmh</groupId>
			<artifactId>jmh-generator-annprocess</artifactId>
			<version>1.37</version>
			<scope>test</scope>
		</dependency>

		<!--
			Canonical RocksDB JNI binding, used by the rocksdb-jni JMH variant
			(see src/test/java-rocksdb/.../RocksDbJniBenchmark.java) for the
			Java-layer baseline that mirrors the Rust criterion rocksdb_compare
			bench. The artifact bundles the platform native library, so no
			external dylib path is required.
		-->
		<dependency>
			<groupId>org.rocksdb</groupId>
			<artifactId>rocksdbjni</artifactId>
			<version>8.11.4</version>
			<scope>test</scope>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<compilerArgs combine.children="append">
						<arg>--add-modules</arg>
						<arg>jdk.incubator.vector</arg>
					</compilerArgs>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>jmh</id>
			<activation>
				<activeByDefault>false</activeByDefault>
			</activation>
			<dependencies>
				<dependency>
					<groupId>org.openjdk.jmh</groupId>
					<artifactId>jmh-core</artifactId>
					<version>1.37</version>
					<scope>test</scope>
				</dependency>
				<dependency>
					<groupId>org.openjdk.jmh</groupId>
					<artifactId>jmh-generator-annprocess</artifactId>
					<version>1.37</version>
					<scope>test</scope>
				</dependency>
			</dependencies>
			<build>
				<plugins>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>build-helper-maven-plugin</artifactId>
						<version>3.4.0</version>
						<executions>
							<execution>
								<id>add-jmh-test-source</id>
								<phase>generate-test-sources</phase>
								<goals>
									<goal>add-test-source</goal>
								</goals>
								<configuration>
									<sources>
										<source>src/test/jmh</source>
									</sources>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>
