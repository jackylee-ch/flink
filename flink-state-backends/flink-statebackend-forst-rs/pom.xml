<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.apache.flink</groupId>
		<artifactId>flink-state-backends</artifactId>
		<version>2.2.0</version>
	</parent>

	<artifactId>flink-statebackend-forst-rs</artifactId>
	<name>Flink : State backends : ForSt-RS (Rust + JDK 25 FFM)</name>

	<packaging>jar</packaging>

	<properties>
		<forstrs.native.libpath>${user.home}/Code/stczwd/ForSt/target/release/libforst_rs_ffi.dylib</forstrs.native.libpath>
		<surefire.module.config><!--
			FFM API requires opt-in for native access in JDK 22+.
			-->--enable-native-access=ALL-UNNAMED
			-Dforstrs.native.libpath=${forstrs.native.libpath}
		</surefire.module.config>
	</properties>

	<dependencies>

		<!-- core dependencies -->

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-streaming-java</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-runtime</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- table-common provides RowData / AsyncTableFunction for the local-lookup function -->
		<dependency>
			<groupId>org.apache.flink</groupId>
			<artifactId>flink-table-common</artifactId>
			<version>${project.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- test dependencies -->

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<!--
			JMH is declared in test scope so that the bench harness under
			src/test/java/org/apache/flink/state/forstrs/jmh/ can pick up
			@Benchmark annotations if we ever wire the JMH maven plugin in.
			The ForStCompareBenchmark.java currently runs as a plain main()
			driven by run-jmh-3way.sh — JMH itself is not strictly required
			to execute the bench, but having the deps available makes the
			JMH-plugin path one config block away.
		-->
		<dependency>
			<groupId>org.openjdk.jmh</groupId>
			<artifactId>jmh-core</artifactId>
			<version>1.37</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.openjdk.jmh</groupId>
			<artifactId>jmh-generator-annprocess</artifactId>
			<version>1.37</version>
			<scope>test</scope>
		</dependency>

		<!--
			Canonical RocksDB JNI binding, used by the rocksdb-jni JMH variant
			(see src/test/java-rocksdb/.../RocksDbJniBenchmark.java) for the
			Java-layer baseline that mirrors the Rust criterion rocksdb_compare
			bench. The artifact bundles the platform native library, so no
			external dylib path is required.
		-->
		<dependency>
			<groupId>org.rocksdb</groupId>
			<artifactId>rocksdbjni</artifactId>
			<version>8.11.4</version>
			<scope>test</scope>
		</dependency>

	</dependencies>

</project>
